{
  "cli": {
    "version": ">= 20.0.0",
    "appVersionSource": "local"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "ios": {
        "simulator": true
      }
    },
    "preview": {
      "distribution": "internal",
      "channel": "preview",
      "ios": {
        "simulator": true
      }
    },
    "production": {
      "_comment": "Build-number policy: buildNumber/versionCode are git-injected by app.config.js (git rev-list --count HEAD) so the Settings build stamp stays ground truth. appVersionSource is 'local' and autoIncrement is OFF; remote autoIncrement would silently override the stamp.",
      "channel": "production"
    }
  },
  "submit": {
    "production": {}
  }
}
