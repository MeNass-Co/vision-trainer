# progress-ring — locked spec (Phase 3)
Canvas detected (reference, `target.png`): 1180×2556 px (@3x → 393.3×852.0 pt — 1 px over the standard 1179 px @3x width for this device class; treated as 393×852 pt)
Canvas detected (our capture, `design/captures/current/today.png`): 1170×2532 px (@3x → 390.0×844.0 pt)

Reference state note: `target.png` shows the Apple Fitness Activity Ring at **0% progress** ("0/240 CAL"). No filled arc segment exists to sample directly — the fill hue is instead sampled from the head‑marker's solid fill (same color the arc would use once >0%). Cap style is not directly visible at 0% (no arc terminus exists) and is locked from `target.md`'s declared winning property + Apple's universal round‑linecap convention for Activity Rings, cross‑checked against the fact that our own current build already renders round caps (see row 4).

All reference pixel measurements sampled with PIL/numpy (median/mode over small patches to defeat the source PNG's palette dithering — it is a `P`-mode/quantized PNG). All our-capture measurements sampled the same way, then fit to a circle via least-squares (Kasa fit) over ~12,000 track+arc pixels — fit residual std = 1.5 px, i.e. sub-pixel accurate.

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|
| **GEOMETRY** | | | | |
| 1 | Ring diameter (centerline) | 414 px outer / 332 px centerline → **138.0 pt outer / 110.7 pt centerline** | **Current (unchanged anchor): 751.8 px → 250.6 pt** centerline diameter | Reference: horizontal scan y=1688 through ring center found outer edges at x=118/x=532 (414 px span), symmetric about x=325. Ours: least-squares circle fit (Kasa) on 11,054 track px + 1,112 arc px → center (584.5, 731.5), radius 375.9 px (σ=1.5 px) → diameter 751.8 px |
| 2 | Stroke width | 82 px → **27.3 pt** | **Current (pre-spec, defect): ≈6 px → 2.0 pt.** **Target (locked): 49.6 pt** (= our diameter 250.6 pt × ratio row 3) | Reference: radial scan at y=1688, track band x119–200 and x450–531, both 82 px wide. Ours: radial profile from fitted center at multiple angles — cyan core 374–378.5 px, track core 374–377.5 px (both ≈4–6 px core, 372–380 px incl. AA) |
| 3 | **Stroke : diameter RATIO (transfers)** | **82⁄414 = 0.198 (≈19.8%, ≈1:5.05)** | **Locked: 0.198** — apply verbatim to our diameter, do not carry the absolute 27.3 pt | Ratio computed from row 1/2 reference pixels; this is the value SPEC-INSTRUCTIONS mandates transfer (not the absolute pt) |
| 4 | Cap style | Round (not visible at 0% in target.png; locked from `target.md` won-property + Apple HIG Activity Ring convention) | **Round — already correct**, verified in current build (see `arc_end.png`/`arc_start.png` crops: both arc termini show a clean round cap) | Crops of today.png at (680–850,330–460) and (520–700,330–420) at 4× nearest-neighbor, visually confirmed round terminus |
| 5 | Ring inner diameter, if stroke grown per ratio 3 (informational) | 250 px hole → 83.3 pt | **Computed: 201.1 pt** (centerline 125.3 pt − stroke/2 24.8 pt = inner r 100.5 pt) | Derived from rows 1–3. Validation note: our orb's own outer edge sits at radius ≈101.7 pt (row 9) — within 1.2 pt of this computed inner edge. Growing the stroke per the transferred ratio, symmetrically about the *unchanged* current centerline, naturally lands the ring snug against the orb rather than floating — same tight ring-to-content relationship Apple has to its tile, achieved without copying Apple's absolute geometry |
| 6 | Head marker shape | Circle | Circle | Visual inspection, `target.png` |
| 7 | Head marker diameter : stroke-width ratio | 83 px ⁄ 82 px = **1.01 (≈1:1)** | **Locked: 1:1 → 49.6 pt** (= row 2 target stroke width) | Reference: bright-pixel mask (R>140,G<60, x<560, y<1600) bbox = 84×82 px, center (325, 1524). Marker center-to-ring-center distance = 166 px = exactly the stroke centerline radius (207+125)/2 — confirms the marker rides centered ON the stroke, not floating outside it |
| 8 | Head marker position on stroke | Rides centered on the stroke centerline, at the ring's current-value angle (here: 12 o'clock / 0%) | Same: centered on stroke centerline, positioned at the arc's leading (clockwise) edge; at 0% progress sits at 12 o'clock | Marker center radius 166 px = ring centerline radius (207+125)/2 = 166 px, exact match |
| 9 | Head marker glyph/icon | **Present**: dark near-black (≈#090302) right-facing chevron/arrow, centered inside the marker | **None** — not carried. `target.md`'s "what we copy" list names only "head marker riding the stroke end," not an inner glyph; this specific arrow reads as Apple's own 0%-state affordance, not a universal ring property | Sampled marker interior at (310,1524): (9,3,2) vs. marker fill (252,18,81) — clearly a separate dark glyph layer, not anti-aliasing |
| 10 | Ring start angle / fill direction | 12 o'clock, sweeps clockwise (Apple convention) | Unchanged — current build already starts at 12 o'clock and sweeps clockwise | Visual: arc pixels in today.png span angles −90.4° to −60.8° from center (measuring from positive x-axis), i.e. starting at top |
| **COLOR & GRADIENT** | | | | |
| 11 | Track hue relationship to fill | Same hue as fill, low opacity — **never grey** | **Current (defect): solid `#1E2A2D` = `tokens.surface.hairline` at 100% opacity — flat neutral grey, violates the "never grey" property.** **Target (locked): `proposed: accent.trackTint = 'rgba(51,210,214,0.15)'`** (= ACCENT hue, 15% opacity) | Reference: track composite (65,27,40) vs. local bg (31,28,33) vs. marker/fill (252,18,81) solves to α≈0.15 (R:0.154, B:0.146 agree; G noisy, small denominator). Ours: sampled current track at multiple angles/backgrounds — composite result stayed pixel-identical (30,42,45) regardless of the varying local bg behind it, which is only possible if it's an **opaque flat color**, not an alpha blend. (30,42,45) = `#1E2A2D` exactly matches `tokens.surface.hairline` |
| 12 | Track opacity | **≈0.15 (15%)** | **Target (locked): 0.15** | Computed alongside row 11 |
| 13 | Fill (progress arc) color | `#FC1251` solid (≈Apple's known Move-ring red `#FA114F`, within PNG palette/dither tolerance) | `tokens.accent.default` **`#33D2D6`** — unchanged, already correct | Reference: median patch at marker top/left/right/bottom (325,1490)/(300,1524)/(350,1524)/(325,1555), all read (252,18,81) exactly. Ours: arc core sampled at multiple angles reads (51,210,214) exactly = `#33D2D6`, invariant of local bg → confirms opaque, already token-correct |
| 14 | Head marker fill color | `#FC1251` solid — same value as fill (marker = 100%-opacity version of the ring's live color) | `tokens.accent.core` **`#5BE9EC`** (new — no marker currently exists) | Token doc: `core` = "the one live element on a screen"; the head marker is the ring's live current-position indicator, so it takes the top of the luminance ladder, one step brighter than the arc's `accent.default` |
| 15 | Local card/background context (not copied — reference only) | `#1F1C21` | n/a — our background is `tokens.surface.*`, unrelated to this element | Reference bg sampled at (700,1688) and (325,2000), both (31,28,33) |
| **MATERIAL** | | | | |
| 16 | Blur / glass | Not present on this element in either reference or ours | N/A | Solid strokes only, no translucent material on the ring itself |
| **TYPE** | | | | |
| 17 | Text | Not present on this element (labels like "Move 0/240 CAL" belong to the parent card, out of scope) | N/A | Element scope is the ring graphic only |
| **SPACING** | | | | |
| 18 | Orb-to-ring gap (our composition only — Apple has no equivalent, its ring has no separate "orb") | N/A (declared adaptation — composition is ours) | **Current: 70.9 px → 23.6 pt.** Post-ratio (row 3/5), computed gap narrows to **1.2 pt** (ring inner edge 100.5 pt vs. orb edge 101.7 pt) | Orb edge found via sharp-brightness-drop scan at y=725 (x=273–284) → orb edge radius ≈305 px = 101.7 pt from fitted ring center; gap = ring inner edge (372 px / 124.0 pt) − orb edge (305 px / 101.7 pt) |
| **STATES** | | | | |
| 19 | 0% progress | Full track visible (no fill arc); marker sits at the 12 o'clock start position | Same behavior: full tonal track visible, marker at 12 o'clock when progress = 0 | Directly observed — this is the exact state target.png shows |
| 20 | >0% progress (in-progress) | Fill arc grows clockwise from 12 o'clock in `accent.default`/Apple-red; track fills the remainder; marker rides the fill arc's leading (clockwise) edge | Same: fill arc in `accent.default`, track (row 11 target) fills remainder, marker (`accent.core`) rides the leading edge | Inferred from Apple's well-documented ring mechanics + row 7/8/10 geometry; NOT directly visible in target.png (0% state) — flagged as inferred, not measured |
| 21 | 100% / lapped | Full circle filled; marker returns to/overlaps the 12 o'clock start point | Same (not directly visible in target.png — inferred from Apple's known ring behavior) | Inferred, flagged — could not be measured from the supplied reference |
| **MOTION** | | | | |
| 22 | Arc fill draw-on animation | Not measurable from a static PNG | **ORCHESTRATOR-SETS** (candidate existing token for reference only: `tokens.motion.timing.drawOnMs` = 900 — not assigned here per instructions) | N/A — static image |
| 23 | Head marker travel/settle animation | Not measurable from a static PNG | **ORCHESTRATOR-SETS** | N/A — static image |

## Return-message summary (for orchestrator)
- Rows: 23 (5 GEOMETRY-adjacent groups collapsed to the format's categories; all must-cover rows present)
- `proposed:` tokens: **1** — `accent.trackTint = 'rgba(51,210,214,0.15)'` (no existing token carries ACCENT hue at 15% opacity; closest existing is `accent.glow` at 30%)
- Rows inferred rather than directly measured (flagged, not silently assumed): row 20 (>0% state) and row 21 (100% state) — target.png only shows 0% progress, so in-progress/complete rendering is inferred from Apple's documented ring mechanics + our own measured geometry, not sampled pixels
- Row 9 (glyph) and rows 22–23 (motion) explicitly NOT carried/NOT assigned, per target.md scope and SPEC-INSTRUCTIONS' motion rule respectively
- Flag for design review: row 2's literal ratio transfer (0.198 × our current 250.6 pt diameter = 49.6 pt stroke) turns our current ~2 pt hairline into a much bolder band. Row 5's validation shows this is spatially coherent (ring lands almost exactly against the orb's edge, gap 1.2 pt) rather than arbitrary — but it is a large visual-weight change from the current build and should get a visual gut-check before implementation, since target.md's "thin orbit" framing was written against the current hairline look
- Path: `/Users/nassimlecornet/projects/vision-trainer/design/references/progress-ring/spec.md`
