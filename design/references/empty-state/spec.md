# empty-state — locked spec (Phase 3)
Canvas detected: 1125×2436 px (@3x → 375×812pt — iPhone X/11 Pro/12/13-mini class canvas)
Source measured: `target.png` (Shake Shack 'Your bag is empty', identical file to curated `candidate-3.png`).
Carve-outs in effect (per `target.md`): our own celestial visual replaces the stick-figure illustration; our own copy replaces "Say what?!"; hue is remapped to our signature cyan accent. Only the STRUCTURAL RHYTHM (proportions, gaps, alignment, type scale, CTA geometry) is copied.

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|

**GEOMETRY**
| 1 | Visual block — top offset | Top of visual group (decorative marks) at y=504px | **168.0pt** = 20.7% of screen height (812pt) | Row-activity scan (pixel diff vs bg `#151515` > threshold), first active row above the illustration cluster, excluding status bar (y52–85) and close button (y170–223). |
| 2 | Visual block — height | y=504→1366px, 863px tall | **287.7pt** = 35.4% of screen height | Same scan; last active illustration row is y=1366 (feet), before the 53px gap to the title. |
| 3 | Visual block — horizontal position | x=382→740px (width 358px), center x=561 vs canvas center 562.5 (Δ1.5px) | **Centered** (Δ0.5pt, negligible) — size our celestial visual to ~119pt wide, centered | Column-activity scan within the visual's y-range. |
| 4 | CTA geometry — height | 135px (y1802→1936) | **primary-button spec locked value: 48.0pt** (Δ −3.0pt vs this reference's measured 45.0pt) | Row-activity scan, button-fill mask (`#6DB340` ±40). Cross-checked against `primary-button/spec.md` row 1. |
| 5 | CTA geometry — corner radius | ~13px, confirmed symmetric at all 4 corners (curve completes over 13px both axes; NOT a stadium — straight sides remain between the curves) | **Reference uses a subtle rounded-rect (≈4.3pt), NOT our locked `radius.pill`.** Cross-check: `primary-button/spec.md` row 2 locks radius.pill (24pt, true stadium). **Delta: our system's CTA is a full pill; this reference's CTA is a barely-rounded rectangle — do not copy the radius, keep radius.pill.** | Circle-fit on fill-mask silhouette at all 4 corners (left/right rows 7–20 of button top, x converges from 58→45 / 1066→1079). |
| 6 | CTA geometry — width / side margins | x=45→1079px (width 1034px), margins 45px/46px (near-symmetric) | **primary-button spec locked value: space.base (16pt)** (Δ +0.8pt vs this reference's measured ~15.2pt — effectively matches) | Row scan at button vertical mid (y=1869), half-max edge crossing on fill mask. |
| 7 | CTA bottom offset (button bottom → canvas bottom) | 500px = 166.7pt (20.5% of screen height); to home-indicator top: 461px = 153.7pt | **Do not copy.** Keep `primary-button/spec.md` row 4 locked value (safeAreaBottom 34pt + space.base 16pt = 50pt). **Delta: reference is +116.7pt more generous** — this is Shake Shack's own footer dead-zone, not a rhythm worth inheriting; app-wide CTA anchoring consistency wins over copying this specific gap. | Same edge-detection as row 4, distance to canvas last row (2435) and to home-indicator band (y2397–2411, confirms standard 8.3pt inset — calibration sanity-check). |
| 8 | Text alignment | All of: illustration center x=561, title-line-1 center x=559, title-line-2 center x=557, caption center x=560.5, button center x=562 — all within 5.5px (1.8pt) of true canvas center (562.5) | **Center-aligned** (measured, not assumed — every text/visual element is independently centered, not left-aligned) | Bounding-box center-x comparison across all 5 element bands. |

**COLOR & GRADIENT**
| 9 | Background | `#151515` (21,21,21) flat, no gradient detected (sampled 5 pts across canvas, identical) | **surface.base `#080A0D`** (Δ darker + cooler — reference is a neutral near-black, ours is a cooler near-black) | Direct pixel sample, 5 locations. |
| 10 | Illustration accent (bag outline / decorative marks) | `#6CB23D` (108,178,61) — lime-green, flat, no gradient | **N/A — carve-out.** Our celestial visual uses its own established palette (`ACCENT_CORE`/`ACCENT_SOFT` per existing hero visuals), not a remap of this hue. | Most-common-color histogram inside illustration bbox. |
| 11 | CTA fill | `#6DB340` (109,179,64) flat, 100% opaque, no gradient | **accent.default `#33D2D6`** (hue rotates lime→cyan; structure — flat, 100% opaque — copied exactly) | Direct pixel sample, center of button, 2 points. |
| 12 | Title text color | `#FFFFFF` pure white | **text.primary `#EFF3F4`** (Δ negligible, near-white) | Mode of near-white pixels inside title bboxes (both lines). |
| 13 | Caption text color | `#FFFFFF` — identical to title, no hierarchy split in reference | **text.secondary `#A7B2B4`** (intentional delta: reference uses one flat white for both title+caption; we introduce a hierarchy split per our own type system — not a measured value, a declared adaptation) | Mode of near-white pixels inside caption bbox; confirmed same as row 12. |
| 14 | CTA label color | `#FFFFFF` on `#6DB340` fill (light-on-mid-green, works in reference) | **text.inverse `#08161A`** (reuse the decision already locked in `primary-button/spec.md` row 9 — our `accent.default` is similarly light/mid-luminance, so a dark inverse label is required for contrast, not white) | Brightest-pixel search inside button bbox; luminance check against fill (both ≈155–176, mid-bright — white-on-white-ish in our case, hence the inverse-label lock). |

**MATERIAL**
| 15 | Blur / glass | None detected — flat opaque black bg, flat opaque green fill, no translucency anywhere in the frame | **Not applicable.** No `material.*` tokens used on this screen; flag `material: n/a` (not `verify-on-glass`) | Full-frame color histogram: only 2 flat fills + anti-aliasing artifacts, no intermediate translucent bands. |

**TYPE**
| 16 | Title — size/weight/line-height (2-line) | Line 1 "Say what?!": cap-height 72px; Line 2 "Your bag is empty.": cap-height 70px (avg 71px). Est. font-size via ≈0.727 cap-height ratio → **≈32.6pt**. Stroke-to-cap-height ratio ≈16.9% (thick — bold/heavy). Line-top-to-line-top (baseline rhythm) = 116px = **38.7pt**. Inter-line gap (descender-to-ascender) = 18px = **6.0pt**. Two-line block total ink height 215px = 71.7pt. | Nearest token: **type.title (fontSize 28, lineHeight 34)** — Δ **+4.6pt size / +4.7pt line-height** (reference runs noticeably larger/looser than our title token; our celestial-visual empty-state should size up from the token default to preserve the "display-size" feel target.md calls for). Weight: nearest **fontFamily.bold** (stroke ratio too heavy for semibold). | Cap-height glyph-column isolation ("S" line 1, "Y" line 2), cropped + measured; stroke width via horizontal scanline run-length at y=1460/1580. |
| 17 | Caption — size/weight | Cap-height 34px ("C" of "Choose"). Est. font-size ≈34/0.727 → **≈15.6pt**. Stroke-to-cap-height ratio ≈14.7% (still fairly bold for a caption, but lighter than the title). Single line — line-height not empirically measurable (no second line to baseline-compare). | Nearest token: **type.body (fontSize 15, lineHeight 24)** — Δ **+0.6pt size**, much closer fit than `type.caption` (13pt, Δ+2.6pt). Weight: nearest **fontFamily.medium** (matches `type.caption`'s existing weight choice, consistent with our system even though size maps to body). | Cap-height glyph-column isolation ("C"), cropped + measured; stroke ratio via scanline at y=1685. |
| 18 | CTA label — size/weight | Cap-height 34px ("F" of "Find") — same cap-height as caption, i.e. same visual size. Est. font-size ≈**15.6pt**. Visually bold/chunky stroke (thick rounded display face). | Nearest token: **type.bodyStrong (fontSize 15, lineHeight 24)** — Δ negligible on size; matches the primary-button system's label register. Cross-check: `primary-button/spec.md` locked label token was **type.heading (20pt)** off a *different* source (Opal, 393pt-canvas) — flag this as a cross-reference inconsistency to resolve at the orchestrator level, not silently pick one. | Glyph-column isolation on white-only mask inside button bbox (excludes green fill). |

**SPACING**
| 19 | Visual-to-title gap | Visual bottom (y=1366) → title-line-1 top (y=1419) = 53px | **17.7pt** — nearest token: between `space.md` (12) and `space.base` (16); closest is **space.base (16pt)**, Δ +1.7pt | Edge-to-edge band distance. |
| 20 | Title-to-caption gap | Title-line-2 bottom (y=1633) → caption top (y=1664) = 31px | **10.3pt** — nearest token **space.sm (8pt)**, Δ +2.3pt (or treat as bespoke `space.md`-adjacent, 12pt, Δ −1.7pt — space.md is the closer of the two on balance) | Edge-to-edge band distance. |
| 21 | Caption-to-CTA gap | Caption bottom (y=1710) → CTA top (y=1802) = 92px | **30.7pt** — nearest token **space.xl (32pt)**, Δ −1.3pt | Edge-to-edge band distance. |
| 22 | Horizontal margins — text block (title + caption container) | Caption spans x=148→973 (width 825px), margins 148px/152px, near-symmetric — title lines are narrower but share the same center, confirming a shared ~150px-margin container | **≈50pt each side** — nearest token **space.xxl (48pt)**, Δ +2.0pt. Notably wider than the CTA's own 15.2pt/16pt margin (row 6) — **the text block is inset far more than the CTA**, which spans almost full width. This asymmetry is a real structural feature to copy, not a measurement artifact. | Bbox edges of caption (widest text line) vs canvas width 1125px. |

**STATES**
| 23 | Screen state | Single static empty-state; no toggles, no enabled/disabled variants visible in this reference (it's a terminal/dead-end screen, not a form) | **N/A** — CTA's own enabled/disabled states are already covered by `primary-button/spec.md` rows 15–19; this element contributes no new state rows | Visual inspection — one screenshot, one state. |

**MOTION**
| 24 | Entrance animation (visual/title/caption/CTA stagger) | Not measurable from a static PNG | **ORCHESTRATOR-SETS** | n/a |
| 25 | CTA press/idle motion | Not measurable from a static PNG — see `primary-button/spec.md` rows 20–21 | **ORCHESTRATOR-SETS** | n/a |
