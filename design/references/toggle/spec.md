# toggle — locked spec (Phase 3)
Canvas detected: 1125×2436 px (@3x → 375×812 pt)
Source crop measured: two ON rows ("Enable Quick Switch" y 1267–1359px, "Pure Black Dark Mode" y 1666–1748px thumb) and two OFF rows ("Reduce Smearing" y 1793–1885px, "PURER Black Dark Mode" y 1930–2012px) on pure-black settings rows. All four instances measured identically within ±1px; values below are the converged reading.

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|
| **GEOMETRY** | | | | |
| 1 | Track width | 153px → 51.0pt | 51.0pt (fixed, stock UISwitch width — untouchable per target.md) | Horizontal pixel scan at track mid-height (y=1313), left edge x=876 (bg #131415 → fill), right edge x=1028 (fill → bg #131415), width = 1028−876+1 = 153px |
| 2 | Track height | 93px → 31.0pt | 31.0pt (fixed, stock UISwitch height) | Vertical pixel scan in flat mid-section (x=930 for ON row, x=982 for OFF row, both outside the thumb's circular footprint), top y=1267, bottom y=1359 (ON); top y=1793, bottom y=1885 (OFF) → 93px both |
| 3 | Track corner radius | Fully rounded stadium/pill — radius = height/2 = 46.5px → 15.5pt | 15.5pt (`radius.pill` semantics — a literal half-height stadium, not `radius.pill: 999` as a raw value but the same visual result) | Verified visually on 4×-upscaled crop: end caps are perfect semicircles equal to half the track height; no straight vertical segment at the caps |
| 4 | Thumb diameter | 81px → 27.0pt | 27.0pt (fixed, stock UISwitch thumb) | Pure-white (255,255,255) bounding-box scan, all 4 instances: width=81px, height=81px identically (x942–1022 / y1273–1353 for ON_1; x882–962 / y1799–1879 for OFF_1) |
| 5 | Thumb inset from track edge (radial, at rest) | 6px → 2.0pt, uniform on all sides | 2.0pt | Vertical: track top 1267 → thumb top 1273 = 6px; thumb bottom 1353 → track bottom 1359 = 6px. Horizontal (OFF, thumb at left cap): track left 876 → thumb left 882 = 6px. Horizontal (ON, thumb at right cap): thumb right 1022 → track right 1028 = 6px. Matches formula trackRadius(46.5px) − thumbRadius(40.5px) = 6px |
| 6 | Thumb travel distance | OFF center x=922 → ON center x=982 = 60px → 20.0pt | 20.0pt | Thumb center computed from bbox midpoints: OFF (882+962)/2=922, ON (942+1022)/2=982. Delta = 60px. Equals trackWidth(153)−trackHeight(93)=60px, confirming end positions sit exactly in the two end-cap centers |
| **COLOR & GRADIENT** | | | | |
| 7 | Track fill — ON | `#67CE67` (RGB 103,206,103), flat, fully opaque | `#33D2D6` (`ACCENT`) — hue carve-out per target.md, structure/opacity copied exactly (flat 100% fill, no gradient) | Sampled at (x=935, y=1313), flat mid-track region away from thumb and shadow, on the "Enable Quick Switch" and "Pure Black Dark Mode" rows (identical value both instances) |
| 8 | Track fill — OFF | `#323336` (RGB 50,51,54), flat, fully opaque | `proposed: surface.controlTrackOff = '#33383D'` — no existing token matches this luminance; nearest existing token is `surface.hairlineStrong` (`#28363A`) but it reads visibly darker/more saturated-cyan, so a dedicated token is proposed at the same luminance as reference, cool-tilted 2–3 units into the theme's blue-grey family to sit believably between `surface.raised` (#0E1316) and `text.muted` (#6E827F) | Sampled at (x=970, y=1840), flat mid-track region on "Reduce Smearing" / "PURER Black Dark Mode" rows (identical value both instances) |
| 9 | Thumb fill — both states | `#FFFFFF` (RGB 255,255,255), flat | `#FFFFFF` — unchanged, target.md: "nothing else changes" | Sampled at thumb center, both ON instances (982,1313 / 982,1709) and both OFF instances (920,1840 / 920,1972): identical pure white in all 4 |
| 10 | Row/settings-card ground | `#131415` (RGB 19,20,21) — the "pure-black row" ground the toggle sits on (not true `#000000`, which only appears outside the row card) | Nearest token `surface.card` (`#12181C`) — visibly close in luminance (delta ≈10 per channel), used as-is, no new token needed since this is row chrome, not the toggle itself | Sampled at (x=855, y=1313) and (x=970,y=1840), both adjacent to but outside all four tracks |
| **MATERIAL** | | | | |
| 11 | Track/thumb translucency | None — both track and thumb are flat opaque fills, no blur/glass | N/A — opaque, `material.*` tokens do not apply to this element | Confirmed: track edges transition directly track-color → row-ground-color with a single 1px antialias step, no soft/blurred falloff consistent with glass |
| **TYPE** | | | | |
| 12 | Text | None — the toggle itself carries no label glyphs (row label is a separate element) | N/A | Visual inspection of all 4 crops |
| **SPACING** | | | | |
| 13 | Thumb inset (see row 5) | 2.0pt uniform | 2.0pt | duplicate ref, see row 5 |
| 14 | Thumb travel (see row 6) | 20.0pt | 20.0pt | duplicate ref, see row 6 |
| **STATES** | | | | |
| 15 | State: ON | Track `#67CE67`, thumb at right end-cap center (x=982 of a 876–1028 track), full opacity, shadow visible (row 17) | Track `ACCENT` (#33D2D6), thumb `#FFFFFF` at right end-cap | Measured directly on "Enable Quick Switch" + "Pure Black Dark Mode" rows |
| 16 | State: OFF | Track `#323336`, thumb at left end-cap center (x=922 of same track), full opacity, shadow not resolvable (row 17 caveat) | Track `proposed: surface.controlTrackOff` (#33383D), thumb `#FFFFFF` at left end-cap | Measured directly on "Reduce Smearing" + "PURER Black Dark Mode" rows |
| 17 | Thumb shadow | Visible only on the ON (green) track — a soft dark ring around the thumb, asymmetric: **absent directly above** the thumb (top edge transitions thumb-white → full track-green `#67CE67` with zero darkened band), **present below** (a `#61C160` band fills the entire 6px/2.0pt gap between thumb-bottom and track-bottom, i.e. offset ≈0, extends the full inset), **present but thinner at the sides** (~3–4px / 1.0–1.3pt band, same `#61C160`, tapering toward the top). `#61C160` vs base `#67CE67` = flat black at ≈6% opacity (computed per-channel: ΔR 5.8%, ΔG 6.3%, ΔB 6.8% — consistent, confirms a neutral black overlay, not a hue shift) | `#000000` @ 6% opacity, blur ≈2.0pt, offset (0, +1.0pt) downward, cast onto the track fill under/behind the thumb only — not cast outward onto the row ground (row 18 confirms no shadow leaks past the track edge) | Pixel-by-pixel transition scan at multiple y-rows through the ON thumb (y=1280/1290/1300/1313/1325/1335/1345, x=935–1032) on "Enable Quick Switch". **Could not confirm on the OFF (grey) track** — the same physical shadow, if present, would darken `#323336` by ~6%, a shift too small to survive this image's palette quantization (image is 8-bit indexed/P-mode); flagging as `material: verify-on-device` for the OFF state specifically |
| 18 | Outer/ambient shadow (track onto row ground) | None — track edge steps directly to row ground `#131415` with a single 1px antialias pixel, no falloff | None — do not add an outer drop shadow | Scanned 15px below/beside the ON_1 track edge (x=950 and x=982, y=1359–1374): flat `#131415` immediately, no gradient |
| **MOTION** | | | | |
| 19 | Thumb slide + track fill crossfade (ON↔OFF) | Not measurable from a static PNG | ORCHESTRATOR-SETS — candidate existing token: `motion.spring.toggle` (stiffness 200, damping 15, mass 1) already defined in `src/theme/tokens.ts`, orchestrator to confirm/assign | N/A, flagged per spec instructions |
