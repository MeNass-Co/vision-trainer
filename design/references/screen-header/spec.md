# screen-header — locked spec (Phase 3)
Canvas detected: 1179×2556 px (@3x → 393.0×852.0 pt — iPhone 14/15/16 Pro class canvas)
Source measured: `target.png` (Flighty "Eyes on the road" announcement, identical file to curated `candidate-1.png`). All chip/title/caption pixels sampled sit on a pure `#000000` background (verified — not a gradient at this y-range; the gradient starts lower, under the caption block).

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|

**GEOMETRY**
| 1 | Outline chip ("iOS 26") outer size | 147×60px = 49.0×20.0pt (x82→228, y205→264) | **49×20pt** (clean values, both sides sampled twice — width/height are exact px/3 integers) | Non-background bbox mask (threshold >8/255) on region x60-250,y180-290; cross-checked at 2 rows/cols. |
| 2 | Outline chip corner radius | ~12px = **4.0pt** (all 4 corners: top-left, top-right, bottom-left agree at 12±1px) | proposed: `radius.xs = 4` (no existing sibling — `radius.sm=8` is 2× too large, would read as more rounded than reference) | Leftmost-x-per-row scan into top-left/bottom-left corners until value stabilizes to the flat-wall x; top-right confirmed via rightmost-x scan. Not a stadium: r≈12px vs height/2=30px. |
| 3 | Filled chip ("PRO") outer size | 114×55px = 38.0×18.3pt (x253→366, y207→261) | **38×18pt** | Same bbox method, fill-color threshold (>8/255), verified at x=310/340 (clear of corner curvature). |
| 4 | Filled chip corner radius | ~9px = **3.0pt** | proposed: `radius.xs = 4` — unify both chip variants on one radius token for build consistency (reference itself varies 3–4px, within measurement noise) | Leftmost-x-per-row scan, top-left corner, x=253 column. |
| 5 | Chip row: vertical alignment | Chip1 vertical center y≈234.5, Chip2 vertical center y≈234.0 → centers coincide despite the two chips having different heights (20.0pt vs 18.3pt) | `alignItems: 'center'` on the chip row container | Midpoint of each chip's measured bbox. |
| 6 | Chip row → left content margin | Chip1 left edge x=82 (27.3pt); title "E" ink starts x=87 (29.0pt); caption "P" ink starts x=86 (28.7pt) | **left margin ≈ 28pt** shared by chip row / title / caption (the ~5px spread is glyph side-bearing, not a real margin difference) | Column-content bbox left edge, 3 independent samples. |

**COLOR & GRADIENT**
| 7 | Outline chip fill | `#171718` solid, mode-sampled n=1080px (region x200-226,y210-260) — equivalent to white @ ~9% opacity composited on black | `surface.card` **#12181C** (nearest neutral token; ΔL negligible, both near-black cool greys) | Pixel-value histogram mode over a text/border-free interior patch. |
| 8 | Outline chip border color/opacity | Peak `#54565A` (84,86,90), mode-sampled n=111px along the full top edge — reads as white @ **≈34% opacity** on black (84/255=0.33, 86/255=0.34, 90/255=0.35) | proposed: `chip.borderOutline = rgba(255,255,255,0.34)` (NEW — `material.hairlineOnGlass` exists at 0.12, too faint to match; no other alpha-hairline token in the ramp) | Perimeter mode sample along full 147px top edge; corroborated by independent left/bottom/right edge scans, all peaking 84–90/255. |
| 9 | Outline chip border width | ~1px @3x ≈ **0.3pt** (hairline) — transition spans 2-3 sampled px but that is anti-alias spread around a 1-device-px stroke, not a true 2-3px line (no plateau at peak value across >1px) | `hairline.px1` (1px) | 1px-step scanline across all 4 edges (left/top/right/bottom), edge-profile shows a single-sample peak, not a sustained plateau. |
| 10 | Outline chip text color | `#FFFFFF` solid (255,255,255) | `text.primary` **#EFF3F4** (Δ negligible, both effectively white) | Direct sample, full-brightness glyph interior pixels. |
| 11 | Filled chip fill | `#F3E9FB` solid, mode n=525px — a near-white pale-lavender fill (not a translucent overlay; too far from any black-composite alpha curve) | proposed: `accent.chipFill = accent.hot` **#CFFAFB** (reuse `ACCENT_HOT` — same "near-white hot core" role in our ladder, hue rotated purple→cyan per carve-out) | Pixel-value histogram mode over interior patch clear of text. |
| 12 | Filled chip text color | `#6A249A` solid (106,36,154), mode n=570px | proposed: `accent.onLight = #0B6A6D` **(NEW token — no existing sibling)**. Our accent ladder (`core`/`default`/`soft`/`muted`) is tuned entirely for light-on-dark rendering; none is dark/saturated enough for text on a near-white fill the way `#6A249A` is dark against `#F3E9FB`. Value derived by holding the ACCENT hue (~180°) and matching the reference's luminance/saturation role. | Pixel-value histogram mode inside "PRO" glyph strokes. |

**MATERIAL**
| 13 | Chip backgrounds | Opaque flat fills, no blur/glass — zero translucency artifacts at any sampled edge (background is pure black outside both pills, with a clean single-step transition) | **N/A** — no material/blur applies to either chip variant | Edge-profile inspection at all 4 chip edges; no gradual bg-bleed characteristic of glass. |

**TYPE**
| 14 | Outline chip type — cap-height / est. size | "O" stem run = 26px = 8.7pt cap-height → est. font-size **≈12.1pt** (cap-height/0.70 SF ratio) | Nearest: `type.caption` (13pt, Δ−0.9pt) or `type.micro` (11pt, Δ+1.1pt) — **recommend `type.micro`**: it's the only token carrying tracking, matching this badge's convention (see row 16) | Column-run height of "O" left stroke, x102-107, chip1 text region. |
| 15 | Outline chip weight | Stem width 6px / cap-height 26px = ratio 0.23 (small-size hinting inflates this vs large text — see row 19 calibration) | `fontFamily.bold` | Stroke-width run-length at y=233 (the "O" verticals), cross-referenced against title/caption ratios for calibration. |
| 16 | Outline chip tracking | Not independently isolable at 12pt/3x resolution; badge reads visually tight, not exaggerated | `type.micro.letterSpacing` (1.4) is likely **too wide** for this reference — recommend **+0.5pt** as a tighter compromise, flagged as an estimate | Visual crop inspection only; no clean glyph-gap measurement possible on mixed-case "iOS 26". |
| 17 | Filled chip type — cap-height / est. size | "P" stem run = 25px = 8.3pt cap-height → est. font-size **≈11.9pt** (same family as row 14) | Same as row 14 | Column-run height of "P" left stroke, x273-278, chip2 text region. |
| 18 | Filled chip weight | Stem width 6px / cap-height 25px = ratio 0.24 | `fontFamily.bold` | Stroke-width run-length at y=234 ("P" stem). |
| 19 | Filled chip tracking | Measured inter-letter ink-gaps in "PRO": P→R gap = 4px (1.3pt), R→O gap = 4px (1.3pt), symmetric | **+1.3pt equivalent gap** — close to `type.micro`'s convention but confirms a real, measurable tracked badge, not neutral-default spacing | Column-sum profile across "PRO" (x270-345), gap = contiguous zero-count run between letter blocks. |
| 20 | Title type size | Cap-height 59px (E-stem AND F-stem both = 59px exactly, 2 independent lines) = 19.7pt → est. font-size **≈27.5-28pt** | `type.title.fontSize` **28 — Δ0, EXACT MATCH** | Column-run height of "E" (line1) and "F" (line2) left stems, x87-97; both lines agree to the pixel. |
| 21 | Title line-height | Baseline-to-baseline (E-stem bottom row 380 → F-stem bottom row 482) = 102px = **34.0pt exact** | `type.title.lineHeight` **34 — Δ0, EXACT MATCH** | Stem-column bottom-row delta between the two title lines (also verified via stem-top delta, identical 102px). |
| 22 | Title tracking | Not independently isolable at this size beyond the exact size/line-height match already found (rows 20-21) | `type.title.letterSpacing` **-0.4 — Δ0 assumed**, justified by the exact match above | No isolatable glyph-gap measurement attempted; inferred from token match. |
| 23 | ⚠ Title weight | Stem width 10-11px / cap-height 59px = ratio 0.17-0.19 → reads as **Bold**, not Semibold | `type.title.fontFamily` is currently **`fontFamily.semibold`** — this is a real mismatch. Recommend a `fontWeight: 'bold'` override for this hero header, OR accept the token's semibold as an intentional softening (flagging for implementer decision, not silently resolving it) | E-stem width (x87-97, 10px) and F-stem width (x87-97, 11px) run-length scans, both at cap mid-height; calibrated against caption's 0.111 ratio (row 26) which correctly reads as the token's own `medium` weight. |
| 24 | Caption type size | "P" stem run = 36px = 12.0pt cap-height → est. font-size **≈17.1pt** (matches iOS system Body default exactly) | Nearest: `type.body`/`type.bodyStrong` (15pt, **Δ+2.1pt**) — reference caption is noticeably larger than our body token | Column-run height of "P" left stem, x86-90, caption line 1. |
| 25 | Caption line-height | Line-top-to-line-top, 3 gaps measured across the 4-line caption block: 66px / 66px / 66px = **22.0pt exact**, identical every time | Nearest: `type.body`/`type.bodyStrong.lineHeight` (24pt, **Δ-2.0pt**) | Full-bbox top row of each of the 4 caption lines (ascender-inclusive), consecutive deltas. |
| 26 | Caption weight | Stem width 4px / cap-height 36px = ratio 0.111 | `type.caption.fontFamily` **medium — matches** (size token is off per row 24, but weight token is correct) | "P" stem width run-length at y=545. |
| 27 | Caption color | `#8D8D92` solid, mode n=6071px (dominant by a wide margin) — equivalent to white @ ~60% opacity on black (iOS `secondaryLabel` dark-mode formula) | `text.secondary` **#A7B2B4** (nearest existing — noticeably brighter/warmer-green than reference's neutral cool grey; consider a dimmer `text.mutedLight` if fidelity matters more than reuse) | Region-wide pixel histogram mode across full caption line 1 (x82-900, y527-573). |

**SPACING**
| 28 | Chip-to-chip gap | 24px = **8.0pt exact** (chip1 right edge 228 → chip2 left edge 253) | `space.sm` **8 — Δ0, EXACT MATCH** | Direct edge-to-edge pixel gap. |
| 29 | Chip-row → title gap | 58px = 19.3pt (chip1 bottom 264 → title "E" cap-top 322) | **~20pt** — falls between `space.md`(12, Δ-7.3) and `space.lg`(24, Δ-4.7); recommend `space.lg` with a -4.7pt visual tightening, or a proposed `space.lg-tight: 20` | Tallest-chip bottom edge to title's cap-top row (first non-bg row of "E" stem). |
| 30 | Title → caption gap | 30px = **10.0pt exact** (title line2 full-bbox descender-bottom row 497 → caption line1 full-bbox ascender-top row 527) | **~10pt** — falls between `space.sm`(8, Δ+2) and `space.md`(12, Δ-2); recommend `space.md` with a -2pt tightening | Full-word bbox (ascender-to-descender) bottoms/tops, not baseline — this is the true visual gap between text blocks. |
| 31 | Outline chip horizontal padding | 20px/side = **6.7pt**, symmetric (pill width 147px, text width 107px, (147-107)/2=20) | `space.sm` **8 — Δ+1.3pt** | Text-ink-bbox to pill-outer-edge, both sides independently measured (20px / 20px). |
| 32 | Outline chip vertical padding | 16px top / 18px bottom = 5.3pt / 6.0pt (slight asymmetry from optical baseline positioning) | proposed: `space.2xs = 6` (between `xs=4` and `sm=8`, no exact sibling) | Text-ink-bbox (cap-height only, 26px) to pill-outer-edge, top and bottom independently. |
| 33 | Filled chip horizontal padding | 20px/side (left) to 23px (right) ≈ 6.7-7.7pt | `space.sm` **8 — Δ+0.8 to +1.3pt** | Text-ink-bbox to pill-outer-edge. |
| 34 | Filled chip vertical padding | 15px/side = **5.0pt exact, symmetric** | proposed: `space.2xs = 6` **(Δ+1pt)**, or accept 5pt as a custom value if exact fidelity matters more than token reuse | Text-ink-bbox (25px) to pill-outer-edge, top and bottom independently, both exactly 15px. |
| 35 | ⚠ Implementation note — chip vertical rhythm | Both chip heights are built from **cap-height**, not full font line-height (chips have no descenders in "iOS 26"/"PRO", so the box was cropped tight to caps) | Do not build chip height as `lineHeight + 2×padding` using `type.caption.lineHeight=18` — that yields ~30pt, 50% taller than reference's 20pt. Lock chip height to a fixed value (20pt / 18pt) or use a tight custom line-box. | Arithmetic cross-check: pill height 60px − text cap-height 26px = 34px padding (17px/side avg) vs the token's line-height-based math which would add ~12px of unused ascender/descender space. |

**STATES**
| 36 | Outline chip — states visible | Only one static state present in the reference (single screenshot, no press/selection shown) | pressed/disabled treatment: **ORCHESTRATOR-SETS** (not measurable from a static PNG) | Visual inspection — one state only. |
| 37 | Filled chip — states visible | Only one static state present | pressed/disabled treatment: **ORCHESTRATOR-SETS** | Visual inspection — one state only. |

**MOTION**
| 38 | Chip/title/caption entrance animation | Not visible/measurable — static PNG | **ORCHESTRATOR-SETS** | n/a per instructions. |
