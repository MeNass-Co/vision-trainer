# tab-bar — curated target (Phase 2)

Source: Breathwrk — Home screen (`candidate-1.png` → `target.png`)

Winner: **Breathwrk — Home** (candidate-1).
Why: speaks our exact grammar — 3 tabs, icon+label, floating dark pill with a frosted active-tab pill — executed with real material: visible blur over content, disciplined label weight, optically-sized filled icons. Highest-fidelity active-state treatment of the set (fill + weight, not color alone).
Harmony: neutral frost material keeps the accent in icon/label tint where our cyan ladder lives; sits naturally over the starfield. Runner-up TIDE had the prettiest glass but a 4-tab, icon-only-active grammar that would force a structural rewrite.
Carve-outs: 3 tabs (count), our labels (text), active tint → ACCENT_CORE ladder (hue).
