# secondary-button — locked spec (Phase 3)
Canvas detected: 1179×2556 px (@3x → 393.0×852.0 pt)
Source crop measured: primary "View Course" pill (y≈2111.5–2267.5px) + secondary "Close" outline pill (y≈2303.5–2459.5px) in `target.png`. All px measured with PIL pixel sampling on the RGB-converted image; every row below states the exact sample coordinate(s).

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|
| **GEOMETRY** |
| 1 | Secondary height | 156.0px = **52.0pt** (top 2303.5 → bottom 2459.5, sampled x=150/300, avoids corner curve) | 52.0pt | Vertical scan, bg-delta threshold >8, edges at first/last non-bg row |
| 2 | Primary height (sibling) | 156.0px = **52.0pt** (top 2111.5 → bottom 2267.5, x=150/300) | — (primary out of scope) | Same method |
| 3 | Height relationship | **IDENTICAL** — 156px vs 156px, 0px delta | secondary = primary height, no independent value | Direct comparison of rows 1–2 |
| 4 | Secondary width | 986px = **328.7pt** (left 96 → right 1082, confirmed at y=2360/2380/2400/2420/2440, all 5 rows agree exactly) | 328.7pt (= screen width − 2×32pt margin) | Horizontal scan at 5 mid-height rows, all identical |
| 5 | Primary width (sibling) | 986px = **328.7pt** (confirmed at y=2150/2180/2210/2230/2250, all 5 rows agree exactly) | — | Same method |
| 6 | Width relationship | **IDENTICAL** — bit-exact match (986px both, 0px delta across 10 sample rows) | secondary = primary width | Direct comparison of rows 4–5 |
| 7 | Corner radius, secondary | ≈18px = **6.0pt** (flat resumes at dy=18 from top-left outer corner, x=300 column scan, y2304→2322) | 6.0pt | Curve-fit: quarter-circle edge-x(y) until flat; flat-onset row minus top-edge row = r |
| 8 | Corner radius, primary (sibling) | ≈17px = **5.7pt** (flat resumes dy=17, top-left corner AND cross-checked at bottom-right corner independently, also dy=17) | — | Same method, 2 independent corners |
| 9 | Radius relationship | **IDENTICAL within AA noise** (17px vs 18px, 1px/0.3pt delta — below single-pixel AA precision at this crop) | 6.0pt shared value | Visual corner crop confirms same curvature by eye (`/tmp/primary_corner.png` vs `/tmp/secondary_corner.png`) |
| 10 | Radius vs our token ramp | reference 5.7–6.0pt | **no exact match**: nearest is `radius.lg`=14 (Δ+8pt, 2.3× too small); `radius.pill`=999 is wrong shape entirely (this is a rounded-rect, NOT a stadium/capsule — corner sweep is 18px into a 156px-tall button, far short of full pill) | Radius fit above |
| 11 | Left margin (screen edge → button edge), both buttons | 96px = **32.0pt** (identical for primary and secondary, both measured at outer edge x=96) | `space.xl` = 32 — **exact match** | Horizontal scan, left edge, both buttons share x=96 |
| 12 | Right margin, both buttons | 97px = **32.3pt** (screen width 1179 − right edge 1082, identical for primary and secondary) | `space.xl` = 32 — near-exact (Δ+0.3pt) | Horizontal scan, right edge, both buttons share x=1082 |
| 13 | Margin relationship | **IDENTICAL** L/R margins between primary and secondary (both buttons share x=96 and x=1082 exactly — 0px delta) | secondary = primary margins = `space.xl` both sides | Direct comparison |
| 14 | Vertical gap, primary bottom → secondary top | 36px = **12.0pt exactly** (2267.5 → 2303.5) | `space.md` = 12 — **exact match** | Outer-edge-to-outer-edge vertical distance |
| **COLOR & GRADIENT** |
| 15 | Secondary border color | `#132745` (19,39,69) — flat, uniform across all sampled points on the stroke (top/left/right/bottom all identical), no gradient detected | Role-match to `surface.hairlineStrong` = `#28363A` (nearest existing neutral-hairline token; `surface.hairline` `#1E2A2D` is the subtler alternative if less contrast is wanted) | Sampled at (150,2305) top border, (96–99,2380) left border, (1079–1082,2380) right border, (150,2457) bottom border — all read `#132745` |
| 16 | Secondary border opacity | Cannot be deconvolved from a single flat backdrop (border only crosses one uniform bg region in this crop — no lighter/darker zone to test alpha vs flat-color hypothesis). Testing "white-at-alpha-over-bg" gives inconsistent per-channel alpha (2.5%–7.7%), so it reads as an **authored flat hex**, not a translucent overlay | Treat as opaque `#132745`-equivalent; on our theme this is a **neutral, non-accent** hairline (confirms target.md note that the pair is "pre-remapped" — border was never accent-tinted, so **no hue rotation needed**, straight role-map to hairline token) | Arithmetic check: (19-13)/(255-13)=2.5% vs (39-21)/(255-21)=7.7% vs (69-40)/(255-40)=13.5% — not a consistent single-alpha white overlay |
| 17 | Secondary interior fill | **Truly transparent** — bit-exact match to surrounding page background `#0D1528` (13,21,40) at every one of 11 interior sample points (x=130,200,300,400,500,600,700,800,900,1000,1040 at y=2380), plus above/below/outside-button controls, **zero pixel variance** | `backgroundColor: 'transparent'` (or `surface.base`/parent bg passthrough) — no fill token needed | 11-point sample sweep, all `(13,21,40)` exactly; also confirmed no gradient (checked y=2280 above button and y=2490 below, same value) |
| 18 | Primary fill (sibling, for contrast reference) | `#51F1DA` (81,241,218), flat, no gradient detected | `ACCENT_CORE` = `#5BE9EC` (hue-remap: near-identical luminance/saturation role, mint-cyan → our cyan ladder core) | Sampled (300,2115) |
| 19 | Secondary label color ("Close") | `#FFFFFF` exact, dominant color in bbox (1655/2071 text px) | `text.primary` = `#EFF3F4` (near-white token; source is neutral white, not accent-tinted → no hue rotation) | Region color-histogram of Close bbox (520,2365)–(657,2403) |
| 20 | Primary label color ("View Course", sibling) | `#0D1428` (13,20,40) — reads as **exact match to the page background hex**, i.e. the app reuses its own base-dark as the "text-on-accent" color | `text.inverse` = `#08161A` (role match: our token explicitly reserved for text on light/accent surfaces) | Region color-histogram of View Course bbox (430,2172)–(744,2211); dominant off-glyph color = page-bg hex |
| **MATERIAL** |
| 21 | Secondary surface treatment | No blur/glass — flat outline pill directly on the screen's solid dark gradient background, no translucency artifact detectable (bit-exact interior match rules out any blur-material sampling) | Not glass; standard flat surface, no `material.*` tokens apply | Confirmed by row 17's zero-variance interior read |
| **TYPE** |
| 22 | Secondary label cap-height | 39px = **13.0pt** (baseline 2403, cap-top 2365, from "C" glyph column group x520–553) | — | Column-wise glyph bbox scan, bg-delta threshold, isolated per-letter groups |
| 23 | Primary label cap-height (sibling) | 39px = **13.0pt** (baseline 2211, cap-top 2172/2173, from "i"-stem and "C" of "Course", both independently 39px) | — | Same method, 2 independent letters cross-checked |
| 24 | Cap-height relationship | **IDENTICAL** (39px both, 0px delta) — same font size on both buttons | shared type spec | Rows 22–23 |
| 25 | Estimated font size | Cap-height 13.0pt ÷ typical 0.70–0.72 cap-height-to-em ratio ≈ **18.1–18.6pt** | Nearest existing ramp entry: `type.heading` fontSize=20 (Δ**−1.6pt**, ~9% smaller than token). No ramp entry lands closer. | Derived from row 22 |
| 26 | Font weight | Stroke-width/cap-height ratio ≈ 7px/39px = 0.179 (measured on "l" stem in "Close") — this ratio reads as **Bold**, clearly heavier than Medium (~0.10–0.12) or Semibold (~0.13–0.15) | `type.heading` uses `fontFamily.medium` — **weight mismatch**, token is too light. `proposed: type.buttonLabel = { fontFamily: fontFamily.bold, fontSize: 18, lineHeight: 22, letterSpacing: -0.2 }` | Column-width measurement on isolated "l" glyph group |
| 27 | Letter-spacing | Cannot resolve sign definitively below ~0.3px/letter-gap precision at this size; visually reads as normal-to-slightly-tight tracking, **not** loose | Nearest token `type.heading.letterSpacing` = −0.2 — adopt as-is, no evidence to deviate | Visual + glyph-gap inspection, `/tmp/primary_label.png`, `/tmp/secondary_label.png` |
| 28 | Label vertical centering, secondary | Glyph box (2365–2403) midpoint 2384.0 vs button center 2381.5 → **0.83pt off-center** (optical, negligible) | center label in button (standard flex-center), no manual offset needed | Arithmetic from rows 1, 22 |
| 29 | Label vertical centering, primary (sibling) | Glyph box (2172–2211) midpoint 2191.5 vs button center 2189.5 → **0.67pt off-center** (optical, negligible) | Same | Arithmetic from rows 2, 23 |
| **SPACING** |
| 30 | Internal horizontal padding | Not separately measurable — label is centered, button width is fixed/full-bleed (row 4), not content-hugging. No left/right label-to-edge padding token applies. | full-width button, label horizontally centered | N/A by construction |
| **STATES** |
| 31 | Enabled/default (visible) | Only state present in the static reference — outline pill, `#132745` border, transparent fill, white label, as measured above | as specced rows 1–29 | Direct — this is the only state in target.png |
| 32 | Pressed | **NOT VISIBLE** in reference (static screenshot, no pressed frame captured) | `ORCHESTRATOR-SETS` — recommend opacity/scale feedback consistent with primary button's pressed state once that spec exists | Cannot measure from single PNG |
| 33 | Disabled | **NOT VISIBLE** in reference | `ORCHESTRATOR-SETS` | Cannot measure from single PNG |
| **MOTION** |
| 34 | Press feedback timing/curve | Not measurable from a static PNG | `ORCHESTRATOR-SETS` (candidate reference: existing `motion.spring.press` token, stiffness 300/damping 20/mass 1, and `motion.pressScale` 0.96 — reuse primary's press feel for consistency, do not invent a new curve) | N/A — flagged per instructions |
