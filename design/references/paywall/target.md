# paywall — curated target (Phase 2)

Source: Mimo — Max paywall (`candidate-2.png` → `target.png`)

Winner: **Mimo — 'Your developer journey starts here'** (candidate-2).
Why: near-exact structural match to our anatomy — badge chip → headline → checkmark feature rows → plan card (badge + selected-border state) → CTA → legal caption ('Cancel anytime on the App Store', literally our caption). Border-change selection state, not color-alone.
Harmony: purple → cyan remap is trivial; check-glyph feature rows replace our soft dots (that IS the amateur gap). Bevel (candidate-1) is the fallback if check rows feel thin.
Carve-outs: ONE plan card (count), our copy (text), hue.
