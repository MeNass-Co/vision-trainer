# onboarding-chrome — locked spec (Phase 3)
Canvas detected: 1179×2556 px (@3x → 393.0×852.0 pt — iPhone 14/15/16 Pro-class canvas)
Source measured: `target.png` (Equinox+ 'tell us about yourself', step 1 of 5, identical file to curated `candidate-1.png`). CTA rendered here is a flat grey placeholder rectangle (disabled-looking, square corners) — it is NOT the button spec; button appearance is fully governed by the locked `primary-button/spec.md`. Rows below for the CTA cover **anchoring only** (position), per task scope.

| # | Property | Reference value | Our value (token/pt/hex) | How measured |
|---|---|---|---|---|

**GEOMETRY — top chrome**
| 1 | Back chevron bounding box (w×h) | 29px×53px (x48–77, y245–298) | **9.7pt × 17.7pt** | Luminance>150 bbox scan, region x30–100/y185–320; stable across thresh 80–200 (±1px). |
| 2 | Back chevron stroke width | Horizontal cut through diagonal arm = 6–7px at 4 sampled rows (y260/270/280/290); arm angle ≈45–48° from vertical (from bbox w29/h53, half-arm atan(29/26.5)=47.6°) → perpendicular stroke = 6.5px×sin(45°) ≈ 4.6px | **≈1.5pt** (perpendicular, angle-corrected) | Row scans at 4 y-values through both diagonal arms, corrected for ~45° line angle (raw horizontal cut overstates true stroke width). |
| 3 | Back chevron color | `#FFFFFF` (255,255,255), pure white, sampled at multiple stroke pixels | **text.primary `#EFF3F4`** (Δ near-imperceptible: reference is saturated pure white, token is 96%-white) | Direct pixel sample, brightest-pixel search in glyph bbox. |
| 4 | Back chevron position | Left ink edge x=48px, top edge y=245px | **left = space.base (16pt)** (measured 16.0pt, exact) · **top = 81.7pt from canvas top** | Column/row edge scan; left matches CTA/progress-bar left margin exactly (see rows 16–17). |
| 5 | Gap below status bar (status-bar content bottom → chevron top) | Status-bar glyphs (clock+icons) bottom edge y≈106px (35.3pt); chevron top y=245px (81.7pt) → gap = 139px | **46.3pt** raw gap. Alt framing: iOS safe-area-top on notch/Dynamic-Island devices = 59pt; chevron's total 81.7pt top offset − 59pt safe-area = **22.7pt in-content top padding** (recommended value to add below `insets.top` in our build) | Bbox scan of clock digits (x30–300,y40–110) and status icons (x850–1140,y40–110) for status-bar bottom; chevron top from row 1. |
| 6 | Step label "1 of 5" — position | Left ink edge x=49px, top y=384px, bottom y=409px | **left = space.base (16pt)**, matches chevron/progress-bar/title margin exactly · top ≈ **128.0pt** from canvas top | Bbox scan x40–140/y370–420, thresh100. |
| 7 | Gap: step-label bottom → progress-bar top | Label bottom y=409/410, bar top y=437 | **9.0pt** | Direct row difference. |

**GEOMETRY — progress bar**
| 8 | Progress bar total (conceptual) width | Filled segment left x=48 to track's visible right terminus x≈1130–1131 (track fades below noise floor past x=1130; matches CTA/button right edge exactly, row 17) | **space.base margins both sides → 393pt canvas − 2×16pt = 361.0pt** (measured 361.0pt, exact match to primary-button spec row 5's 361.3pt button width, Δ0.3pt) | Row scan y=438 (track row) for right terminus; cross-checked against CTA button edges (row 16–17), identical x-positions. |
| 9 | Progress bar height — filled segment | 15px (y437–451), flat 255 luminance across entire height at every sampled column inside fill | **5.0pt** | Column scan x=60,150 (clear of edges), luminance step-function top/bottom. |
| 10 | Progress bar height — track (unfilled) | 3px (y437–439), top-aligned with filled segment's top edge (NOT vertically centered on it) | **1.0pt**, top-aligned | Column scan x=700/900/1100 (clear track zone), found via diff-from-background sweep after initial null result at fill's vertical mid-row (track sits at top of the fill's height, not centered — verified by comparing row y=444 [mid] which reads as pure background vs row y=438 [top] which reads as track). |
| 11 | Progress bar corner radius | Both ends of filled segment: perfectly flat left/right edges across all 15 rows of height (no curvature at any sampled y, thresholds 80–200) | **0 / square-cut** — no rounding detected at this scale; reference reads as sharp rectangle, not a pill | Per-row leftmost/rightmost bright-x scan, y435–454; constant x at every row = zero curvature. |
| 12 | Progress bar fill fraction | Filled width = 217px (x48–264) of 1083px conceptual total = **20.04%** — matches step "1 of 5" almost exactly (1/5 = 20.0%) | **currentStep / totalSteps** (formula, not hardcoded — count is a target.md carve-out: our onboarding may have N≠5 steps) | filled_width / (right_edge − left_edge), both directly measured. |
| 13 | Progress bar — filled color/opacity | `#FFFFFF` (255,255,255), 100% opaque | **PROPOSED design decision: ACCENT `#33D2D6` @ 100%** — reference is monochrome white, but per tokens.ts's own role doctrine ("core = the one live element on a screen"), the growing fill is exactly that live element; hue-remap carve-out (target.md) licenses this. **Flagged: not reverse-engineered, a design call.** | Direct pixel sample (fill), diff-test confirms 0 variance across fill. |
| 14 | Progress bar — track color/opacity | Track pixel `(53,47,49)` vs local background `(26,7,14)` at same x → per-channel alpha-blend-white inversion: R 0.118, G 0.161, B 0.145, mean **α≈0.14** | **material.hairlineOnGlass `rgba(255,255,255,0.12)`** (Δ−0.02 opacity, near-exact) | Alpha-inversion from track px vs background px sampled at identical x (x=700), 3 channels averaged. |

**GEOMETRY — Skip affordance**
| 15 | Skip — bounding box | Text ink (incl. "p" descender) x1004–1097, y252–296. Cap-height (S) = y252–287 = 35px. **No underline present** — a suspected underline on first visual pass was disproven by pixel scan: it was the "p" descender stem (x1074–1078 only, not full-width); confirmed zero signal beyond y296 across x990–1110. | **type.bodyStrong-class label, no underline** | Full luminance-array dump (thresh-free) of region y280–300/x990–1110 to rule out a false-positive underline seen in an upscaled NEAREST-resize crop. |
| 16 | Skip — position | Right ink edge x=1097px; canvas width 1179px → margin = 82px. Vertical center y≈274px, vs chevron center 271.5px (Δ0.8px, i.e. same row) | **right margin ≈ 27.3pt** — closer to **space.lg (24pt, Δ+3.3pt)** than space.base (16pt, Δ+11.3pt); glyph ink typically under-reports true text-box edge by a few px of font side-bearing, so space.lg is the safer target · vertically centered on chevron row | Bbox scan + canvas-width subtraction; vertical center compared to row 1's chevron center. |

**TYPE**
| 17 | "1 of 5" — size/weight | Cap-height (digit "1") = 25px (y384–409) = 8.33pt → est. fontSize ≈ **11.7pt** (÷0.714 SF-Pro cap-height ratio). Stroke ratio ≈ 2.5px/25px = 10% → medium-weight class | Nearest by raw size: type.micro (11, Δ+0.7) — but micro carries letterSpacing 1.4 (wide-tracked caps label), which this is not (sentence-case, tight spacing). **Recommend type.caption (13, Δ−1.3, medium weight ✓, letterSpacing 0 ✓)** — style match outweighs the smaller size-delta. | Cap-height bbox of digit "1", horizontal stroke-width scan through its stem (y396). |
| 18 | Skip — size/weight | Cap-height (S) = 35px (y252–287) = 11.67pt → est. fontSize ≈ **16.3pt** (÷0.714). Stroke ratio (k-stem) ≈ 5px/35px = 14.3% → semibold-leaning, heavier than the step label | Nearest token: **type.bodyStrong (15, Δ+1.3)** — size is the closest available; token's medium weight is one step lighter than measured (semibold-leaning), accepted as closest existing option (no semibold token exists at this size) | Cap-height bbox of "S", stroke-width scan through "k" vertical stem (y265). |
| 19 | Title — size/weight/line-height | Cap-height ("F") = 50px (y571–621) = 16.67pt → est. fontSize ≈ **23.35pt** (÷0.714). Stroke ratio (F-stem) = 7px/50px = 14% → semibold. Line spacing (line1 cap-top 571 → line2 ascender-top 658) = 87px = 29.0pt → line-height/fontSize ratio = 1.242 | Nearest by raw size: type.heading (20, Δ+3.35) is numerically closer than type.title (28, Δ−4.65) — **but weight (14% stroke = semibold, matches title's `fontFamily.semibold`; heading is medium) and line-height ratio (1.242 vs title's 34/28=1.214, Δ0.028; vs heading's 26/20=1.3, Δ0.058) both corroborate type.title over type.heading.** Recommend **type.title (28/34, semibold, Δ−4.65pt on raw size only)**. | Cap-height bbox of "F" (line1) and ascender-top of "b" in "about" (line2, no cap letter available on that line), stroke-width scan through F-stem (y590). |
| 20 | Caption — size/weight/line-height | Cap-height ("T") = 29px (y786–815) = 9.67pt → est. fontSize ≈ **13.54pt** (÷0.714). Stroke ratio (T-stem) ≈ 3px/29px = 10.3% → medium. Line spacing (line1 top 786 → line2 top 841) = 55px = **18.3pt** | Nearest token: **type.caption (13, Δ+0.54)** — near-exact size match; measured line-height 18.3pt vs token lineHeight 18, **Δ+0.3pt, near-exact**; medium weight matches token's `fontFamily.medium` exactly. Best-fidelity type match in this spec. | Cap-height bbox of "T", stroke scan (y800), line-band top-edge comparison between the two caption lines. |
| 21 | Title/caption color | Title `#FFFFFF` (pure white, F-stem sample) · Caption `(154,153,153)` ≈ `#999999`, flat/neutral grey (T-stem sample, non-AA pixel) | Title → **text.primary `#EFF3F4`** (Δ minimal) · Caption → **text.secondary `#A7B2B4`** (Δlum: measured 153 vs token's grayscale-equiv ~172, −19; text.muted `#6E827F` lum~124 is Δ−29, farther) — secondary is the closer luminance sibling and already carries the app's cool-cyan hue remap | Direct pixel samples on non-anti-aliased stroke pixels (avoids AA edge underestimation). |

**SPACING**
| 22 | Title block top offset | Title cap-top y=571px = **190.3pt** from canvas top · relative to progress-bar bottom (y=451): gap = 120px = **40.0pt** | Use the **relative** figure (40.0pt below progress-bar bottom) as the build anchor — robust to status-bar height variance across devices, unlike the absolute canvas-top figure | Row 1 cap-height bbox top vs row 9's bar-bottom edge. |
| 23 | Title-to-caption gap | Title block bottom (last line, incl. descenders: "yourself." with 'y' descender + period) y=722px → caption block top y=786px | **21.3pt** | Band-detection scan (contiguous bright-row runs, thresh90) across y470–900, isolating 4 text bands; gap = band3.top − band2.bottom. |
| 24 | Caption block internal line-height | See row 20 | **18.3pt** (≈type.caption lineHeight 18) | Row 20. |

**GEOMETRY — CTA anchoring** (cross-ref `primary-button/spec.md` for button appearance; rows below are anchoring/position only)
| 25 | CTA bottom offset (canvas bottom → button bottom) | This reference (Equinox): button bottom y=2439px, canvas bottom 2556px → gap = 117px = 39.0pt | **Use the LOCKED primary-button spec value instead: safeAreaBottom (34pt) + space.base (16pt) = 50pt** (primary-button spec row 4, measured 50.17pt from its own Opal source). Δ vs this reference: **−11.17pt** — this reference sits closer to the screen bottom than our locked button anchor; disregard, build to the locked 50pt figure for cross-app consistency. | Column scan x=200 (inside button, clear of text) for top/bottom edges; compared directly against primary-button spec.md row 4. |
| 26 | CTA full-width margins (L/R) | Left edge x=48px, right edge x=1131px → 48px/16.0pt each side | **space.base (16pt) each side** — Δ0 vs primary-button spec row 3 (its 47.5px/15.83pt is the same measurement within rounding). Exact match confirms one shared margin system across chrome, progress bar, and CTA. | Row scan y=2350 (button mid-height) for left/right edges; identical x-positions to progress-bar row 8 and chevron row 4. |

**STATES**
| 27 | Progress bar fill-vs-track contrast (the only "state" visible: filled=complete, track=remaining) | Filled 100% white opaque vs track ~14% white — Δ86pp opacity | **ACCENT 100% (fill) vs hairlineOnGlass 12% (track) — Δ88pp**, same contrast relationship preserved | Rows 13–14. |
| 28 | Skip — enabled only (no disabled/pressed state visible in this reference) | Single static state, 100% opacity | **100%, text.primary** — no other state to spec from this PNG | Visual confirmation only; flag if a disabled Skip state is needed elsewhere, not measurable here. |

**MOTION**
| 29 | Step-to-step progress-bar fill transition | Not measurable from a static PNG | **ORCHESTRATOR-SETS** | n/a |
| 30 | Screen-to-screen chrome transition (chevron/back nav) | Not measurable from a static PNG | **ORCHESTRATOR-SETS** | n/a |
