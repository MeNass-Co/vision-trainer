# onboarding-chrome — curated target (Phase 2)

Source: Equinox+ — '1 of 5' onboarding step (`candidate-1.png` → `target.png`)

Winner: **Equinox+ — 'tell us about yourself'** (candidate-1).
Why (pixels over scout's centered Beside pick): back chevron + '1 of 5' step label + thin progress bar at TOP-left + left-aligned title/caption + anchored CTA — matches both the 5-app top-placement convergence (our bottom line is the outlier) and our left-anchored type system.
Harmony: Mindvalley's visible disabled-CTA (candidate-5's sibling in the primary-button set) informs the CTA state spec.
Carve-outs: our step count (count), our copy (text), hue.
