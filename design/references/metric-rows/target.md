# metric-rows — curated target (Phase 2)

Source: WHOOP — Sleep statistics rows (`candidate-1.png` → `target.png`)

Winner: **WHOOP — sleep statistics rows** (candidate-1).
Why: icon + caps label, big value right with delta arrow, muted baseline beneath — the exact upgrade path for our Vision-profile rows (right anatomy, missing encoding layer).
Harmony: one WHOOP voice across the progress screen.
Declared secondary source: Apple Stocks (candidate-3) for ONE sub-component only — sparkline proportion inside a row (WHOOP has none).
Carve-outs: our labels/values (text), hue.
