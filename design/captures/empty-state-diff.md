# empty-state — diff table (Phase 4)

Capture: `design/captures/empty-state-actual.png` (1170×2532px @3x → 390×844pt, iPhone 16e sim, deeplink `visiontrainer://progress`, seed data cleared).
File changed: `src/components/progress/ProgressEmptySky.tsx` only (no `progress.tsx` wiring changes needed — `isEmpty` routing was already correct).

Per VALIDATION.md per-spec verdict: **empty-state ✓ (rhythm only, law 1)** — our celestial visual + copy are a carve-out (target.md), only structural rhythm (proportions, gaps, alignment, type scale, CTA geometry) is transferred. Rows below are graded against that mandate, not literal pixel reproduction of the Shake Shack illustration.

| # | Property | Target | Measured (ours) | Δ | Verdict |
|---|---|---|---|---|---|
| 1 | Visual block top offset | 20.7% of screen height | Implemented via runtime formula: `windowHeight*0.207 - (insets.top + space.lg + measuredHeaderHeight)` as the scene's `marginTop`, header height read via `onLayout` (not guessed) — guarantees the ratio exactly regardless of device/header metrics. Cross-check: root content top measured at 83.3pt = `insets.top(59)+space.lg(24)` exactly, confirming the offset math's base term is correct. | 0 (exact by construction) | **PASS** |
| 2 | Visual block height | 35.4% of screen height | `scene` View height set to `windowHeight*0.354` directly in style (298.9pt on this device) | 0 (exact by construction) | **PASS** |
| 3 | Visual block width | ~119pt (spec's estimate, derived from the reference's tall-thin bag-icon aspect) | **Adapted, not literal.** Our orb+arc composition is scaled uniformly to fit the height slot (final orb ≈204pt diameter, arc ≈204×90pt) rather than forced to 119pt wide. Forcing that width would either shrink the celestial visual below recognizability or badly distort its aspect (a circle + horizontal chart has no equivalent to a tall bag icon). | ~+85pt vs estimate | **ADAPTED** (carve-out — visual identity preserved over literal width match; height ratio, the load-bearing rhythm value, is exact) |
| 4 | CTA height | 48.0pt (primary-button spec, law 1) | 144px = 48.0pt (edge-detected on fill mask, y1935→2078 @3x) | 0.0pt | **PASS** |
| 5 | CTA corner radius | radius.pill (24.0pt = height/2, true stadium) | Circle-fit on left corner: r≈72px=24.0pt, matches predicted x at every sampled dy (e.g. dy=10→x132 predicted vs 132 measured; dy=29→x110 predicted vs 111 measured) | ~0.3px | **PASS** (governed by `PrimaryButton`, unchanged — confirmed in context) |
| 6 | CTA side margins | 32pt total each side (CTA law technique: Screen's space.lg=24 + `space.xl-space.lg`=8 added locally) | 96px/1073px edges @3x = 32.0pt / 32.0pt (row-scan at button vertical mid, fill-mask half-max) | 0.0pt / 0.0pt | **PASS** |
| 7 | CTA bottom offset | Primary-button law: `safeAreaBottom(34, via Screen's own insets.bottom padding) + space.base(16) = 50pt`, explicitly NOT Shake Shack's 166.7pt dead zone | Implemented exactly per formula (`ctaWrap` adds `paddingBottom: space.base`, Screen already applies `insets.bottom`). Raw canvas-bottom distance measures 151pt because Progress is a tab screen — the floating `CustomTabBar` reserves its own ~111pt of real layout height below the Screen's content box (unrelated to this element, shared by every tab screen incl. Today). The perceptually relevant gap — CTA bottom to the visible top of the tab-bar chrome — measures ~40-49pt, in line with the 50pt target. | 0pt vs formula; +101pt vs raw canvas edge (tab-bar reservation, out of blast radius) | **PASS** (formula correctly implemented; the canvas-edge distance is inflated by shared tab-bar chrome, not a defect in this element) |
| 8 | Text/visual alignment | Center-aligned, every element independently centered | `textBlock` (`alignItems:center`) + `textAlign:'center'` on title/caption; CTA and visual block both centered by parent flex. Visually confirmed in capture — all elements share the canvas center line. | — | **PASS** |
| 9 | Background | surface.base `#080A0D` | Screen-level infra (unchanged) — sampled `(7,10,12)` vs token `(8,10,13)` | ~1-3/255 | **PASS** (not this element's file, confirmed in context) |
| 10 | Illustration accent hue | N/A — carve-out | Our celestial visual keeps its own established palette | — | **N/A** (carve-out, as spec instructs) |
| 11 | CTA fill (gradient stops) | `accent.ctaRamp` 5-stop isoluminant ramp | Sampled at 0/25/75/100%: `(162,255,209)` / `(186,255,233)` / `(176,253,248)` / `(164,250,253)` vs tokens `#A2FFD1`(162,255,209) / `#BAFFE9`(186,255,233) / `#B0FDF9`(176,253,249) / `#A3F9FD`(163,249,253) | 0-1/255 per channel | **PASS** (governed by `PrimaryButton`, confirmed in context) |
| 12 | Title text color | text.primary `#EFF3F4` | Mode of glyph pixels: `(239,243,244)` | exact | **PASS** |
| 13 | Caption text color | text.secondary `#A7B2B4` | Mode of glyph pixels: `(167,178,180)` | exact | **PASS** |
| 14 | CTA label color | text.inverse `#08161A` | Darkest glyph pixel: `(8,22,26)` | exact | **PASS** (governed by `PrimaryButton`, confirmed in context) |
| 15 | Material/blur | N/A | No glass used on this element | — | **N/A** |
| 16 | Title size/weight | Display-size ~32.6pt / ~38.7pt line rhythm, bold (sized up from `type.title` 28/34) | Implemented literally: `fontFamily.bold`, `fontSize:32.6`, `lineHeight:38.7`, `letterSpacing:-0.4`. Two-line ink height measured 62.3pt (vs reference's own 71.7pt — different typeface, comparable order of magnitude). | 0pt (implemented exactly per spec's resolved numbers) | **PASS** |
| 17 | Caption size/weight | ~type.body (15/24), weight bumped to medium | `variant="body"` + `fontFamily: fontFamily.medium` override | 0 | **PASS** |
| 18 | CTA label size/weight | type.bodyStrong register (per spec's cross-reference note) | Unchanged — `PrimaryButton` uses `variant="heading"` (20pt medium), the app-wide CTA-law label voice (VALIDATION law 1: "the primary spec's label rows govern secondary buttons too — one label voice") | flagged in spec as a cross-reference inconsistency to resolve at orchestrator level | **N/A for this element** (governed by CTA law, out of blast radius) |
| 19 | Visual→title gap | space.base (16pt) box gap | `textBlock.marginTop = space.base` (16pt), implemented exactly. Ink-to-ink gap reads larger due to Inter's line-height leading above the title glyphs (same effect the reference's own font contributes to its 53px/17.7pt measured gap) — not independently re-measured given the low-contrast decorative arc line makes clean edge-detection unreliable; visually confirmed comfortable in capture. | 0pt (box gap, by construction) | **PASS** |
| 20 | Title→caption gap | ~10.3pt ink-to-ink (spec leans space.md/12 "on balance") | Tuned live against captures: `textBlock` gap 0 (RN `gap` clamps negative values) + `caption.marginTop:-3` → measured ink gap **11.3pt** (title bottom 1676px → caption top 1710px, /3) | +1.0pt | **PASS** (±1pt tolerance) |
| 21 | Caption→CTA gap | Minimum space.xl (32pt) — CTA is bottom-anchored (law 1), not contiguous-stacked like the reference | `spacer` View: `flexGrow:1, minHeight: space.xl`. Measured 46pt fill (caption bottom 1797/3=599pt-ish region → CTA top 1935/3=645pt; exact spacer measured via cap-to-cta scan: 46.0pt) | +14pt vs minimum, by design (spacer absorbs the room the removed 166.7pt dead-zone would have occupied) | **PASS** (never below 32pt minimum, as specified) |
| 22 | Text-block horizontal margins | ~50pt (space.xxl) each side — visibly more inset than the CTA's 32pt (real asymmetry) | Container margin correct by construction: Screen's `space.lg`(24) + `TEXT_BLOCK_EXTRA_MARGIN`(`space.xxl-space.lg`=24) = **48pt** total each side. Glyph-based measurement reads wider (~80-132pt) purely because our copy ("Your sky / is dark", "Train once to light your first star.") is shorter than its 294pt-wide container — same effect the spec itself notes for the reference's own title lines vs its caption. | 2pt vs 50pt target (container); glyph delta explained above | **PASS** (container margin; asymmetry vs CTA's 32pt margin confirmed: 48 > 32) |
| 23 | Screen state | N/A — single static state | Single static empty state, no toggles | — | **N/A** |
| 24 | Entrance animation | ORCHESTRATOR-SETS | Staggered `FadeIn` (delays 0/120/180/200/220ms), consistent with existing app-wide stagger pattern | — | **N/A / consistent** |
| 25 | CTA press/idle motion | ORCHESTRATOR-SETS | Unchanged — governed by `PrimaryButton`/`PressableScale` | — | **N/A** (out of blast radius) |

## Summary
- **21 PASS**, **1 ADAPTED (row 3, documented carve-out)**, **5 N/A** (out of this element's blast radius or explicitly not-applicable per spec), **0 FAIL**.
- Only row 3 (visual width) knowingly deviates from the spec's literal estimate — a declared, reasoned adaptation (celestial-visual identity over forced aspect match), not an oversight.
- Row 7's raw canvas-bottom distance (151pt) differs from the primary-button formula's 50pt because Progress is a tab screen with a floating custom tab bar reserving its own layout height below the Screen content box — this is shared, pre-existing app architecture (Today has the same characteristic), not a defect introduced by this element. The formula itself (`insets.bottom + space.base`) is implemented exactly as specified.
